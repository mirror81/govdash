services:
  app:
    build: .
    container_name: mirante-painel
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    driver: bridge
